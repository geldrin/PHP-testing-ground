server.modules = (
	"mod_access",
	"mod_alias",
	"mod_compress",
	"mod_redirect",
	"mod_rewrite",		# VSQ
	"mod_status",		# VSQ: LVS reachability test
	"mod_setenv",		# VSQ
)

server.document-root        = "/var/www/videosquare.eu/httpdocs_static"
server.upload-dirs          = ( "/var/cache/lighttpd/uploads" )
server.errorlog             = "/var/log/lighttpd/error.log"
server.pid-file             = "/var/run/lighttpd.pid"
server.username             = "www-data"
server.groupname            = "www-data"
server.bind		    = "172.18.1.114"
server.port                 = 80

index-file.names            = ( "index.php", "index.html" )
url.access-deny             = ( "~", ".inc" )
static-file.exclude-extensions = ( ".php", ".pl", ".fcgi" )

compress.cache-dir          = "/var/cache/lighttpd/compress/"
compress.filetype           = ( "application/javascript", "text/css", "text/html", "text/plain" )

## Videosquare CONFIG START

server.network-backend      = "writev"
server.dir-listing          = "disable"

# Static file handling: handlefile.php
url.rewrite = (
  "^/files/(.+/attachments/.+)/([^/]+)$" => "/handlefile.php?file=$1&filename=$2",
  "^/files/(.+)$"                        => "/handlefile.php?file=$1",
  "^/(.+)_v[a-f0-9]+(.+)?(?:\?(.*))?$"   => "/$1$2"
)

# MAIN ORG site: vsq-static.padabudapest.hu
$HTTP["host"] =~ "^vsq-static*\.padabudapest\.hu$" {
  server.document-root = "/var/www/videosquare.eu/httpdocs_static"
  setenv.add-response-header += (
    "Access-Control-Allow-Origin" => "http://video.padabudapest.hu"
  )
}

# Debug requests
#debug.log-request-handling = "enable"

## Videosquare CONFIG END

# default listening port for IPv6 falls back to the IPv4 port
include_shell "/usr/share/lighttpd/use-ipv6.pl " + server.port
include_shell "/usr/share/lighttpd/create-mime.assign.pl"
include_shell "/usr/share/lighttpd/include-conf-enabled.pl"
